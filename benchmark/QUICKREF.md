# 快速参考

## 触发条件

### ✅ 会触发体积对比的操作

1. **创建 PR** 到 main 分支
   ```bash
   git checkout -b feature/my-feature
   # 进行修改...
   git push origin feature/my-feature
   # 然后在 GitHub 上创建 PR
   ```

2. **更新已有 PR** (推送新提交)
   ```bash
   git commit -m "更新功能"
   git push origin feature/my-feature
   ```

3. **重新打开已关闭的 PR**

4. **直接推送到 main 分支**
   ```bash
   git push origin main
   ```

### 🔍 查看结果

- **Pull Request**: 在 PR 的评论区查看自动发布的体积对比评论
- **Push to main**: 在 Actions 标签页 → "Bundle Size Comparison" 工作流日志中查看

## 本地测试

在提交 PR 前，可以本地运行体积对比：

```bash
# 完整流程：打包 + 对比
npm run build:compare

# 仅对比（需要先打包）
npm run size:compare
```

## 工作流文件

- `.github/workflows/size-compare.yaml` - 独立的体积对比工作流
- `.github/workflows/ci.yaml` - 主 CI 工作流（包含体积对比步骤）

## 常见问题

**Q: 为什么我的 PR 没有看到体积对比评论？**
A: 检查 GitHub Actions 是否成功运行，确保仓库有 `pull-requests: write` 权限。

**Q: 如何修改体积变化警告阈值？**
A: 编辑 `benchmark/size-compare.js` 中的第 127 和 133 行，将 `5` 改为你想要的百分比。

**Q: 评论会重复创建吗？**
A: 不会，工作流会智能检测已有评论并更新，而不是创建新评论。

**Q: 可以对比其他文件吗？**
A: 可以，修改 `benchmark/size-compare.js` 中的 `localFilePath` 和 `cdnUrl` 变量。
